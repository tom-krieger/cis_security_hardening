{
  "description": "Check users' dot files are not group or world writable.",
  "input_method": "environment",
  "parameters": {
    "stig": {
      "description": "Check for strickter STIG permissions.",
      "type": "Enum[y,n]",
      "default": "n"
    }
  }
}